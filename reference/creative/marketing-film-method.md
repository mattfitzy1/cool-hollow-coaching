# Marketing Film Method → moved to the `/marketing-video` skill

This method is now a named, repeatable skill. The full pipeline, the
client-feedback revision loop, the costs table and the gotchas live with it:

- **Skill:** `.claude/skills/marketing-video/SKILL.md` — the SOP + one-shot checklist
  + the feedback loop. Invoke with `/marketing-video {brand}`.
- **Deep reference:** `.claude/skills/marketing-video/reference/method.md` — the why,
  the costs table, the `hf result_url` recovery playbook, and the gotchas.
`/marketing-video` makes cinematic ad films for a brand (Seedance i2v,
music-led, no voice).

(This file is kept as a pointer because `/creative` references it; the content moved
to the skill.)
